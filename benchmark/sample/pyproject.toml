[project]
name = "tinyshop"
version = "0.4.2"
description = "A tiny e-commerce demo for documentation testing"
authors = [{ name = "TruthGap" }]
dependencies = [
  "fastapi>=0.110.0",
  "pydantic>=2.5.0",
  "click>=8.1.0",
  "httpx>=0.27.0",
]

[project.scripts]
tinyshop = "tinyshop.cli:main"
